name: Build Mod

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.8'

      - name: Build mod
        run: cd attribute-swap-indicator && gradle build

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: attribute-swap-indicator-mod
          path: attribute-swap-indicator/build/libs/*.jar
